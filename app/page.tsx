'use client'
import Navbar from '@/components/ui/Navbar'
import React from 'react'
// import { useUser } from '@/context/user'

function page() {
  // const { user } = useUser();

  return (
    <div className=''>
      <Navbar/>
    </div>
  )
}

export default page
